!!! 5
%html(lang="en")
  %head
    = render 'layouts/metahead'

  %body
    = render 'layouts/nav'

    .container
      .row
        .span12
          %p
            - flash.each do |key, value|
              = content_tag(:div, value, :class => "round flash #{key}")

          %main
            = yield


// The dark realms:
:javascript

  // TODO: extract to appropriate place:
  $(document).ready(
    // Activating Best In Place
    function() { jQuery(".best_in_place").best_in_place(); }
  );
